# Handoff: Trinity v2.4 — Plan (execution)

*(Written by iterate-plan's Converge sub-flow, 2026-08-21. Named `.exec-handoff-prompt.md` rather than the default `.handoff-prompt.md` because that path already holds this plan's authoring handoff — the committed evidence-base record cited in the plan's References. Do not overwrite or delete that file.)*

## Summary
Trinity v2.4 ships two phases out of the 2026-08-21 doc-bot retrospective. Phase 0: five small evidence-backed fixes — `create-plan`'s register-seeding prompt becomes unconditional (with an explicit commit-before-review boundary), both loop skills gain a fold-time hygiene checklist (shared claims-vs-behavior sweep; iterate-plan-only invariant walk), `iterate-review` gets a production/non-production scope classifier with an experimental reduced loop budget (default 3, confirmed), `iterate-plan` gets open-question freezing (N=3, confirmed, with full counting semantics and human overrides), and both skills gain fold-provenance instrumentation (`introduced_by_pass` + a shared `passes` row schema with run-level `scope_class` in the state files) — the data half of issue #6, with no stop machinery. Phase 1 ports v2.3.0's proportionality machinery into `iterate-plan`: the `accepted-risk` lifecycle and accounting table, register-aware lens inputs composed from the HEAD blob (four load result states specified), the decision-card contract with the plan-shaping-fold escalation, all parity-pinned.

## Plan
Full plan at: `/Users/adminformatics-pm/code/trinity-skills/docs/trinity-v2-4-2026-08-21.md`
Read this first. The review converged at pass 3 (APPROVE ×2); all five open questions are confirmed by Kyle (see the Open questions intro note and the three HISTORICAL pass blocks).

## Current step
Phase 0 (all five §1–§5 items land as one coordinated change set on branch `kyle/trinity-v2-4`). Phase 0 is Iterate-review: YES — run `iterate-review --scope=branch` when its commits are in.

## Shipped commits referenced by the plan
- `e98cc16` — plan + authoring-handoff committed (branch `kyle/trinity-v2-4`)
- `d16f7ef` — pass 1 fold (8 findings)
- `8f63e97` — pass 2 fold (3 findings, all fold-caused by pass 1)
- `c4ca3b2` — pass 3 fold (APPROVE ×2, register-load taxonomy)

## Remaining risks
- The live-symlink hazard is active *during* execution: `~/.claude/skills/*` track the checked-out commit, so keep all work on `kyle/trinity-v2-4` and avoid running concurrent reviews in downstream repos during the merge window (plan's Rollback section).
- R2/R3 are the ones execution can silently violate: don't let the reduced budget become a hard cap (it's a default; explicit `--max-passes` always wins), and keep the hygiene checklist to its two items.
- Phase 0 §5 ships **no stop machinery** — nothing may consult `introduced_by_pass` for control flow; acceptance greps for exactly this.

## First action
Open `create-plan/SKILL.md` and implement Phase 0 §1: make Step 3's accepted-risk seeding prompt an unconditional fourth question for both plan types, and add the commit-before-review boundary note (a seeded entry is review-visible only once committed — plan Approach §1/§6).
